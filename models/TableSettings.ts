export interface TableSettings {
  data:                       any
  columns:                    any
  isLoading:                  boolean
  showPagination?:            boolean
  showVisibleColumnSelector?: boolean
}